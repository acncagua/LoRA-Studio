fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0
tomli-w>=1.0,<2.0
pillow>=10.0,<12.0
tensorboard>=2.16,<3.0

# Optional real embedding providers. Install only when using Machine Review
# with real CLIP embeddings instead of the mock provider.
# transformers>=4.40,<5.0
# torch>=2.0
# open-clip-torch>=2.24,<3.0
